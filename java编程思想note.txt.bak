操作符
   1.直接常量 
         16进制数适用于所有整数数据类型，以前缀0x(0X),后面跟随0 -9或小写(或大写)的a-f来表示。
   2,基本数据类型                 最小值        最大值
        char 2字节（16-bit）     Unicode 0     Unicode  2^16 -1 
	byte 1字节（8bits）       -128             +127 
	boolean  未指定             
	short  2字节（16bits）     -2^15           +2^15-1
	int      4字节  (32bits)      -2^31            +2^31-1 
	long    8字节（64bits）     -2^63           +2^63-1
	float    4字节（32bits）     IEEE754            IEEE754 
	double  8字节（64bits）     IEEE754            IEEE754 
   2.移位操作符
        左移位操作符 << 讲操作符左边的操作数向左移动(在低位补0)。
        "有符号"右移位操作符 >> 讲操作符左边的操作数向右移动：若符号为正，在高位插入0，若符号为1，在高位插入1。
        右移位操作符(>>>)，无论正负，都在高位插入0
    3.三元操作符 if-else
          i>10?i*100:I*10;
流程控制 
     Foreach语法
           for(int i :f){//定义一个int类型的变量i,讲每一个f的元素赋值给i
	   }
      break 强制退出循环  
      continue  停止执行本次循环，之后继续下一次循环
      switch
 初始化与清理
      重载：在类中可以创建多个方法，他们具有相同的名字，但具有不同的参数和不同的定义，也体现了Java的多态性。
      (方法)重写(覆盖)：子类定义了与父类相同的方法名和参数，子类可以继承父类中的方法，而不需要重新编写相同的方法。
      
      构造器
          如程序没有自己定义一个构造器的话，编译器会自动帮你创建一个默认构造器，如果已经定义了一个构造器，编译器就不会帮你自动创建默认构造器。